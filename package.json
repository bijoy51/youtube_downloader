{
  "name": "youtube-downloader",
  "version": "1.0.0",
  "description": "YouTube Video and Audio Downloader",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "pip install yt-dlp || pip3 install yt-dlp || echo 'yt-dlp installation skipped'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
